{
  "name": "ArtistehubServer",
  "version": "0.1.0",
  "description": "Server for Artistehub application",
  "private": true,
  "main": "server.js",
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "dev": "nodemon server.js",
    "build": "npm run build",
    "start": "NODE_ENV=production node server.js",
    "start:prod": "NODE_ENV=production node build/server.js",
    "lint": "eslint ."
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "mongoose": "^8.4.4",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "encoding": "^0.1.13",
    "eslint": "^8.0.0",
    "nodemon": "^2.0.0"
  }
}
